---
import BaseLayout from '../layouts/BaseLayout.astro';
---
<BaseLayout title="mindi.weik">
  <main style="padding:2rem">
    <h1 style="font-family:var(--font-display);color:var(--accent-blog)">it builds.</h1>
    <p style="font-family:var(--font-mono)">tokens + fonts loaded.</p>
  </main>
</BaseLayout>
